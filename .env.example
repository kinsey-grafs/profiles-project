# Grafana Cloud environment variables (default / preferred setup)
# Copy to .env and replace placeholders with values from your Grafana Cloud stack.

# --- Traces, Metrics, Logs (Grafana Cloud → OpenTelemetry) ---
# Use the OTLP endpoint from Connections → OpenTelemetry (handles traces, metrics, logs).
# Format: https://otlp-gateway-XXXXX.grafana.net or https://tempo-prod-XXXXX.grafana.net
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-XXXXX.grafana.net
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic YOUR_BASE64_CREDENTIALS

# --- Profiles (Grafana Cloud → Pyroscope / Profiles) ---
PYROSCOPE_URL=https://pyroscope-XXXXX.grafana.net
PYROSCOPE_BASIC_AUTH_USER=YOUR_STACK_USER_ID
PYROSCOPE_BASIC_AUTH_PASSWORD=YOUR_API_KEY

# --- Option: Apps send to Alloy, Alloy forwards to Grafana Cloud ---
# Use alloy/config.alloy with GRAFANA_CLOUD_* env vars,
# then set: OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# --- Common ---
# OTEL_SERVICE_NAME=api-gateway   # when running gateway
# OTEL_SERVICE_NAME=backend      # when running backend
BACKEND_URL=http://localhost:3001

# Disable logs export (optional)
# OTEL_LOGS_ENABLED=false
