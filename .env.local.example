# Local development - copy to .env when running with docker compose
# These are optional; defaults work for docker compose + local apps

# OTLP endpoint (for traces) - when using docker compose, apps send to localhost
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Pyroscope (profiles) - when using docker compose
PYROSCOPE_URL=http://localhost:4040

# Service names (optional)
# OTEL_SERVICE_NAME=api-gateway
# OTEL_SERVICE_NAME=backend

# Backend URL for the gateway (when running gateway locally, backend elsewhere)
BACKEND_URL=http://localhost:3001

# Disable metrics (optional)
# OTEL_METRICS_ENABLED=false

# Disable logs export to OTLP/Loki (optional)
# OTEL_LOGS_ENABLED=false
